const React = require('react')

class Layout extends React.Component {
    render() {
        return (
            <html>
                <head></head>
                <body>
                    Layout
                </body>
            </html>
        )
    }
}

module.exports = Layout
